note: Returns the number of 1's in the binary representation of a non-negative integer.

    Args:
    x: Non-negative integer

    Returns:
    Number of 1's in the binary representation of x
task HammingWeight using (x as int)
    deliver count(to_bin(x), "1")
