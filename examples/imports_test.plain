rem: Test all three types of imports in use: statement

use:
    assemblies:
        System
        System.Collections
        MyCustomAssembly
    modules:
        io
        io.files
        math
        string.utils
    tasks:
        io.files.ReadBinary
        io.files.WriteBinary
        math.advanced.CalculatePI

task Main()
    display("Testing imports")
    
    rem: Use imported tasks
    var data = ReadBinary("test.dat")
    var pi = CalculatePI(100)
    
    display("Done")

